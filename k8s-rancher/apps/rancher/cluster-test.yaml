apiVersion: provisioning.cattle.io/v1
kind: Cluster
metadata:
  name: test
  labels:
    #  key: string
  namespace: fleet-default
  annotations:
    ui.rancher/badge-icon-text: TST
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true # Sync-wave does not work. Sync-wave is only supported on type 'Application'. Use this instead
spec:
  kubernetesVersion: 'v1.34.5+k3s1'
  rkeConfig:
    machineGlobalConfig:
      disable:
        - servicelb
        - traefik
        - local-storage
      etcd-expose-metrics: true
      disable-etcd: false
      tls-san:
        # - k8s03d.dev.renklus.ch
        - test.k8s.renklus.ch